spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/meter_db
    username: user
    password: ylab
  liquibase:
    change-log: classpath:db/changelog/liquibase-changelog.xml
    defaultSchema: my_custom_schema
